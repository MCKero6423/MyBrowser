# 内存给 1024M (留给系统 1GB 活命)
org.gradle.jvmargs=-Xmx1024m -Dfile.encoding=UTF-8

# 开启 AndroidX
android.useAndroidX=true

# 【关键】禁止多线程并行编译 (省内存)
org.gradle.parallel=false

# 【关键】禁止守护进程常驻 (编译完立马释放内存)
org.gradle.daemon=false

# 【关键】开启构建缓存
org.gradle.caching=true
